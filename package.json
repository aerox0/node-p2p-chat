{
	"name": "p2p-chat",
	"version": "1.0.0",
	"description": "",
	"main": "src/main.ts",
	"scripts": {
		"dev": "ts-node src/main.ts",
		"build": "tsc --project tsconfig.json",
		"assembly": "npm run build && pkg dist/main.js --out-path build",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "aerox0",
	"license": "ISC",
	"devDependencies": {
		"@types/node": "^17.0.0",
		"pkg": "^5.5.1",
		"ts-node": "^10.4.0"
	}
}
